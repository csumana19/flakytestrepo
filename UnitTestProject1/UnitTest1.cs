using Microsoft.VisualStudio.TestTools.UnitTesting;
using System;

namespace UnitTestProject1
{
    [TestClass]
    public class UnitTest1
    {

        [TestMethod]
        public void RandomNumber_IsOddOrEven_Test1()
        {
            // Arrange
            var random = new Random();
            int number = random.Next(1, 101); // Generates a number between 1 and 100

            // Act
            bool isEven = number % 2 == 0;

            // Assert
            // This assertion will always pass, but you can use Debug.WriteLine to see the value
            Assert.IsTrue(isEven);
        }

        [TestMethod]
        public void RandomNumber_IsOddOrEven_Test2()
        {
            // Arrange
            var random = new Random();
            int number = random.Next(1, 101); // Generates a number between 1 and 100

            // Act
            bool isEven = number % 2 == 0;

            // Assert
            // This assertion will always pass, but you can use Debug.WriteLine to see the value
            Assert.IsTrue(isEven);
        }

        [TestMethod]
        public void RandomNumber_IsOddOrEven_Test3()
        {
            // Arrange
            var random = new Random();
            int number = random.Next(1, 101); // Generates a number between 1 and 100

            // Act
            bool isEven = number % 2 == 0;

            // Assert
            // This assertion will always pass, but you can use Debug.WriteLine to see the value
            Assert.IsTrue(isEven);
        }

        [TestMethod]
        public void RandomNumber_IsOddOrEven_Test4()
        {
            // Arrange
            var random = new Random();
            int number = random.Next(1, 101); // Generates a number between 1 and 100

            // Act
            bool isEven = number % 2 == 0;

            // Assert
            // This assertion will always pass, but you can use Debug.WriteLine to see the value
            Assert.IsTrue(isEven);
        }
        [TestMethod]
        public void RandomNumber_IsOddOrEven_Test5()
        {
            // Arrange
            var random = new Random();
            int number = random.Next(1, 101); // Generates a number between 1 and 100

            // Act
            bool isEven = number % 2 == 0;

            // Assert
            // This assertion will always pass, but you can use Debug.WriteLine to see the value
            Assert.IsTrue(isEven);
        }
        [TestMethod]
        public void RandomNumber_IsOddOrEven_Test6()
        {
            // Arrange
            var random = new Random();
            int number = random.Next(1, 101); // Generates a number between 1 and 100

            // Act
            bool isEven = number % 2 == 0;

            // Assert
            // This assertion will always pass, but you can use Debug.WriteLine to see the value
            Assert.IsTrue(isEven);
        }

        [TestMethod]
        public void RandomNumber_IsOddOrEven_Test7()
        {
            // Arrange
            var random = new Random();
            int number = random.Next(1, 101); // Generates a number between 1 and 100

            // Act
            bool isEven = number % 2 == 0;

            // Assert
            // This assertion will always pass, but you can use Debug.WriteLine to see the value
            Assert.IsTrue(isEven);
        }

        [TestMethod]
        public void RandomNumber_IsOddOrEven_Test8()
        {
            // Arrange
            var random = new Random();
            int number = random.Next(1, 101); // Generates a number between 1 and 100

            // Act
            bool isEven = number % 2 == 0;

            // Assert
            // This assertion will always pass, but you can use Debug.WriteLine to see the value
            Assert.IsTrue(isEven);
        }

        [TestMethod]
        public void RandomNumber_IsOddOrEven_Test9()
        {
            // Arrange
            var random = new Random();
            int number = random.Next(1, 101); // Generates a number between 1 and 100

            // Act
            bool isEven = number % 2 == 0;

            // Assert
            Assert.IsTrue(isEven);
        }
    }
}
